name: Trigger paired replay registry final verification

on:
  pull_request:

permissions:
  contents: read

jobs:
  marker:
    if: false
    runs-on: ubuntu-24.04
    steps:
      - run: echo unreachable
